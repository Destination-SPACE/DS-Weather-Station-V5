# West manifest pointing to external Zephyr installation
manifest:
  # Don't download Zephyr - use existing installation
  projects: []
  
  # Use external Zephyr base
  defaults:
    remote: local
    
  remotes:
    - name: local
      url-base: file://

  self:
    path: .